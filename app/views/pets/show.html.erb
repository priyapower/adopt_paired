<!DOCTYPE html>
<html lang="en">
<head>
<title>CSS Template</title>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<style>
* {
 box-sizing: border-box;
}

body {
 font-family: Arial, Helvetica, sans-serif;
}

/* Style the header */
header {
 background-color: #;
 padding: 10px;
 text-align: center;
 font-size: 20px;
 color: white;
}

/* Create two columns/boxes that floats next to each other */
ul {
  list-style-type: none;
  margin: 0;
  padding: 0;
  overflow: hidden;
}

li {
  float: left;
}

li a {
  display: block;
  padding: 8px;
  background-color: #dddddd;
}
</style>
</head>
<body>

  <header>
    <h1>Welcome to Adopt Don't Shop</h1></b>
    <h3>A Denver Based Pet Adoption Website</h3>
  </header>
<body>


<section id="nav-bar">
 <nav>
   <ul>
     <li><a href="/shelters">All Shelters</a></li>
     <li><a href="/pets">All Pets</a></li>
     <li><a href="/favorites">My Favorites(<%= @favorites.count %>)</a></li>
   </ul>
 </nav>
</section>

<section id="pet-show">
  <h2>Meet <%= link_to "#{@pet.name}", "/pets/#{@pet.id}", method: :get %></h2></b>
  <%= image_tag(@pet.image) %>
  <p>LOCATED AT: <%= link_to "#{@pet.shelter.name}", "/shelters/#{@pet.shelter_id}", method: :get %></p>
  <p>Approximate age: <%= @pet.approximate_age %></p>
  <p>Gender: <%= @pet.sex %></p>
  <p>Description: <%= @pet.description %></p>
  <% if @pet.status %>
    <p>Status: Adoptable </p>
  <% else %>
    <p>Status: Pending Adoption </p>
  <% end %></b>
  <h4><%= link_to 'Update Pet', "/pets/#{@pet.id}/edit" %></h4>
  <h4><%= link_to 'Delete Pet', "/pets/#{@pet.id}", method: :delete %></h4>
  <h4><%= button_to 'Favorite This Pet!', "/favorites/#{@pet.id}", method: :patch %></h4>
</section>


</body>
</html>
